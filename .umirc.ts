import { defineConfig } from 'dumi';

export default defineConfig({
  title: 'Blog',
  mode: 'site',
  publicPath: '/blog/',
  locales: [['zh-CN', '中文']],
  // more config: https://d.umijs.org/config
});
